<Page x:Class="PasswordManager.Pages.PasswordsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SoundsPage">
    <Grid>
        <Border CornerRadius="5" Background="{DynamicResource SecundaryBackgroundColor}"/>
        <ScrollViewer HorizontalScrollBarVisibility="Auto">
            <Grid>
                <TextBlock Margin="-380 0 0 0"
                           Width="400"
                           TextAlignment="Left"
                           FontSize="25"
                           x:Name="txtPasswords" 
                           Height="450"
                           Text=""/>
                <TextBlock Margin="-380 -50 0 0"
                           Width="400"
                           Height="450"
                           FontSize="25"
                           Text="Your Passwords"
                           TextAlignment="Center"
                           FontWeight="Bold"/>
                <Button Margin="400 -300 0 0"
                        x:Name="btn_add_password"
                        Style="{StaticResource MaterialDesignFlatMidBgButton}"
                        materialDesign:ShadowAssist.ShadowDepth="Depth0"
                        Height="53"
                        Width="300"
                        materialDesign:ButtonAssist.CornerRadius="10"
                        FontSize="18"
                        Content="Add Password" Cursor="Hand" Click="btn_create_password">
                </Button>
                <Button Margin="400 -150 0 0"
                        x:Name="btn_remove_all_passwords"
                        Style="{StaticResource MaterialDesignFlatMidBgButton}"
                        materialDesign:ShadowAssist.ShadowDepth="Depth0"
                        Height="53"
                        Width="300"
                        materialDesign:ButtonAssist.CornerRadius="10"
                        FontSize="18"
                        Content="Remove All Passwords" Cursor="Hand" Click="btn_remove_all_passwords_click">
                </Button>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>